type Query {
  users: [User!]!
}

input CreateUserInput {
  name: String!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
  deleteUser(id: ID!): User
}

type User {
  id: ID!
  email: String
  name: String!
  posts: [Post]!
}

type Post {
  id: ID!
  title: String!
  author: User
}