type Query {
  users: [User!]!
}

type Mutation {
  createUser(name: String!): User!
  deleteUser(id: ID!): User
}

type User {
  id: ID!
  email: String
  name: String!
  posts: [Post]!
}

type Post {
  id: ID!
  title: String!
  author: User
}